# LLM Agent Amazon Shopping Demo Requirements

# Core dependencies
python-dotenv==1.0.0
openai>=1.0.0

# Sentience SDK (for Demo 1)
# Install with: pip install -e ../ (from playground directory)

# Browser automation (for Demo 2)
# NOTE: Using latest Playwright (1.57+) for macOS compatibility
playwright>=1.57.0

# Video generation - MoviePy 1.x
moviepy==1.0.3
Pillow>=9.0.0,<10.0  # MoviePy 1.0.3 is incompatible with Pillow 10+
decorator==4.4.2
imageio==2.31.1
imageio-ffmpeg==0.4.9

# Utilities
requests==2.31.0
transformers>=4.36.0
accelerate>=0.25.0
torch>=2.1.0
huggingface_hub>=1.3.2
